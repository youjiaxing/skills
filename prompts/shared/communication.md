# 沟通、命名与交付格式

- 默认使用中文与用户沟通。
- 需要用户选择时，直接在回复中给出从 `1` 开始编号的选项，不调用 Agent 自带的提问工具。将推荐选项放在第 `1` 项，说明推荐理由；对其他选项说明不采纳的理由。
- 使用了 Skill 时，在交付说明中列出 Skill 名称。
- Git 提交信息默认采用 `<type>(<scope>): <message>` 格式，其中 `type` 与 `scope` 使用约定标识，`message` 使用中文；例如 `feat(prompts): 增加个人全局提示词`。
